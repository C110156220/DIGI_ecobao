# 環飽 EcoBǎo backend — 正式執行所需的完整相依套件
# Full runtime dependencies (MySQL).
#
# 注意：mysqlclient 需要先安裝 MySQL client library 才能編譯，
#   macOS : brew install mysql-client pkg-config
#   Debian: sudo apt-get install python3-dev default-libmysqlclient-dev build-essential
# 若只想跑測試或 demo（SQLite），改裝 requirements-dev.txt 即可，不需要 MySQL。

-r requirements-base.txt

# Database driver (MySQL)
mysqlclient==2.2.4
# 開發用資料填充腳本 (scripts/seed/) 使用 PyMySQL
PyMySQL==1.1.1
